apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portal-minecraft
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard
  selector:
    matchLabels:
      app: minecraft
      appid: portal
